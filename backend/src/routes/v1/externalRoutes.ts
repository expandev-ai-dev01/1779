/**
 * @summary
 * External API routes configuration
 * Defines public endpoints accessible without authentication
 *
 * @module routes/v1/externalRoutes
 */

import { Router } from 'express';

const router = Router();

export default router;
